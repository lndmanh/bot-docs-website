{
  "name": "bot-docs-website",
  "version": "4.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "clean": "node scripts/build.mjs --clean",
    "preview": "npx serve build"
  },
  "engines": {
    "node": ">=18.0"
  }
}